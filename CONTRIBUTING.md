# Contributing

1. Create a focused branch from `main`.
2. Keep media processing logic separate from Android UI code.
3. Add or update unit tests for FFmpeg argument construction.
4. Run `gradle testDebugUnitTest assembleDebug` before opening a pull request.
5. Do not commit copyrighted test media. Use small self-created fixtures when instrumentation tests are added.
